-- A Script that lists all the records of the table second_table of the database hbtn_0c_0. The rows should have the name value. Result should be in desceding.
SELECT * FROM second_table ORDER BY name DESC;
