-- A Script that lists all the records of the table second_table of the database hbtn_0c_0. The rows should have the name value. Result should be listed in descending score.
SELECT name FROM second_table ORDER BY score DESC;
