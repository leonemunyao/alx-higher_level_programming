-- A script that lists all records of the table second_table from the database hbtn_0c_0
SELECT * FROM second_table;
-- A script that displays the score of the record with the highest score in the table second_table of the database hbtn_0c_0
SELECT MAX(score) FROM second_table;
