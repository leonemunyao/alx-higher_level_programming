-- A script that lists all records of the table second_table from the database hbtn_0c_0. Reseult should display both score and name columns. Redocrd should be ordered by score in descending order.
SELECT score, name FROM second_table ORDER BY score DESC;
