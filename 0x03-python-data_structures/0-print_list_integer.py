#!/usr/bin/python3

def print_list_integer(my_list=[]):
    # Print each element of a list as an intege
    for py_list in range(len(my_list)):
        # Iterate over each element of the list
        print("{:d}".format(my_list[py_list]))